# Database Configuration for Local Development
# Use LOCAL_DATABASE_URL for local development to avoid conflicts with Replit's DATABASE_URL
LOCAL_DATABASE_URL="postgresql://postgres:123456@localhost:5432/docuflow"

# Environment
NODE_ENV=development

# Object Storage Configuration (Required for file uploads)
# For local development, use dummy values - file uploads will be disabled
PRIVATE_OBJECT_DIR="./local-storage/private"
PUBLIC_OBJECT_SEARCH_PATHS="./local-storage/public"

# Google Cloud Storage (Optional - for production)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_CLOUD_BUCKET_NAME=your-bucket-name